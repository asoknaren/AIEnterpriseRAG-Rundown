# Ingestion Loader

Batch ingestion CLI for sending processed document artifacts to the Enterprise RAG Vector API.

Implementation begins with validated settings in `src/config.py`. See `specs/02_ingestion_loader_checklist.md` for the development checklist.